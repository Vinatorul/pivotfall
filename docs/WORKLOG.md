# Журнал работы

Краткая история решений и реализованных этапов проекта.

## 27 июля 2026 — исследование направления

- создан локальный Git-репозиторий;
- сравнены 12 игр Game Boy, Game Boy Color и NES;
- зафиксированы ограничения по масштабу и использованию чужой IP;
- первоначальным ориентиром выбран небольшой action-puzzle в духе Mole Mania.

Подробности сохранены в [RESEARCH.md](RESEARCH.md).

## 27 июля 2026 — переход к одноэкранной аркаде

- выбран более компактный формат в духе классических одноэкранных аркад;
- прямой урон заменён на отбрасывание в ямы и ловушки;
- шарниры могут выключать, поворачивать и перемещать платформы;
- определены патрульный, толкач и стрелок;
- вражеские атаки подчинены общим правилам и могут активировать механизмы;
- MVP ограничен восемью аренами и сроком 6–8 недель.

Полные правила находятся в [CONCEPT.md](CONCEPT.md).

## 27 июля 2026 — каркас Godot-проекта

- создан минимальный проект Godot 4;
- добавлена одноэкранная graybox-арена с твёрдыми границами;
- добавлен персонаж с горизонтальным движением и прыжком;
- для визуальной проверки используются только примитивы движка;
- атаки, противники, опасности и активные платформы пока не реализованы.

Следующим шагом было запланировано базовое отбрасывание и патрульный.

## 27 июля 2026 — удар и патрульный

- добавлен короткий удар без прямого урона;
- удар имеет читаемую зону, короткое активное окно и перезарядку;
- противник получает фиксированный горизонтальный и вертикальный импульс;
- добавлен болванчик, который ходит туда-сюда и разворачивается у края или стены;
- игрок и противник не блокируют друг друга корпусами;
- движение, прыжок и отбрасывание проверены в headless-режиме.

Следующий шаг — добавить яму, устранение противника окружением и перезапуск
арены.

## 27 июля 2026 — яма и полный цикл арены

- в правой части пола сделана хорошо заметная яма;
- падение устраняет противника без очков здоровья и прямого урона;
- падение игрока считается поражением;
- после победы или поражения арена автоматически загружается заново;
- добавлен ручной restart на `R`;
- интерфейс показывает цель и текущее состояние арены.

Следующий шаг — добавить первый шарнир и включаемую платформу, не меняя
существующие правила импульса.

## 27 июля 2026 — шарнир и включаемый мост

- зона удара теперь одинаково взаимодействует с телами врагов и шарнирами;
- добавлен шарнир с короткой блокировкой повторной активации;
- видимый кабель связывает шарнир с конкретным мостом;
- после предупреждения мост переключается между твёрдым и неконфликтным
  состояниями;
- выключенный мост остаётся видимым как полупрозрачный контур;
- противник или игрок на выключенном мосту падает в яму;
- повторный удар возвращает мост.

Следующий шаг — отделить базовое обучение от механизма на две короткие арены
либо перейти к толкающему противнику.

## 27 июля 2026 — две учебные арены

- общий sandbox разделён на две самостоятельные сцены;
- Arena 01 обучает только удару и отбрасыванию в открытую яму;
- Arena 02 вводит шарнир и включаемый мост;
- контроллер арены получил настраиваемый путь к следующей сцене;
- победа в Arena 01 автоматически переводит в Arena 02;
- поражение и ручной restart загружают заново только текущую арену;
- прохождение обеих сцен проверено игровыми ударами, а не прямыми вызовами
  внутренних методов.

Следующий шаг — добавить толкающего противника и использовать его атаку в
третьей арене.

## 27 июля 2026 — центральная яма и толкач

- яма перенесена в середину всех трёх арен и делит пространство на две стороны;
- мост в Arena 02 расположен точно над центральной ямой;
- игрок получил общий интерфейс импульса и короткую блокировку управления после
  вражеского удара;
- добавлен толкач с читаемой подготовкой, коротким рывком и уязвимой паузой
  после промаха;
- рывок использует те же правила импульса и может задевать игрока, противников
  и шарниры;
- Arena 03 обучает уклонению от рывка и ответному столкновению толкача в яму;
- переходы Arena 01 → Arena 02 → Arena 03 и оба исхода третьей арены проверены
  игровыми действиями.

Следующий шаг — добавить поворотную платформу и собрать на ней Arena 04.

## 27 июля 2026 — поворотная платформа

- добавлена платформа с двумя дискретными состояниями: вертикальная стена и
  горизонтальный мост;
- перед поворотом показывается контур будущего положения и цветовой сигнал;
- во время анимации обе коллизии отключаются, а после неё включается только
  форма нового состояния;
- игрок и противники в зоне поворота получают предсказуемый импульс в сторону
  безопасного пола вместо скрытого раздавливания; направление зависит от
  целевого состояния платформы, а не от взгляда персонажа;
- Arena 04 требует создать мост, перейти на правую сторону, снова открыть яму и
  столкнуть в неё патрульного;
- яма в Arena 04 шире максимальной дальности обычного прыжка, поэтому механизм
  нельзя случайно обойти;
- патрульный ходит по отдельной верхней площадке справа и не может перейти мост
  раньше игрока или оказаться с ним по разные стороны закрытой стены;
- с края горизонтального моста можно достать тот же шарнир повторным ударом до
  начала подъёма стены;
- переход Arena 03 → Arena 04 и полный игровой цикл новой арены проверены
  реальными ударами.

После игрового теста это решение было пересмотрено в пользу воздушной
катапульты.

## 27 июля 2026 — воздушная катапульта вместо второго моста

- после игрового теста отказались от переключения «стена ↔ мост»: оно слишком
  близко повторяло механику Arena 02;
- платформа перенесена в воздух и больше не соединяет стороны центральной ямы;
- патрульный ходит прямо по настилу катапульты;
- настил поднят выше максимальной высоты удара в прыжке, поэтому механизм нельзя
  обойти атакой снизу;
- удар по шарниру после короткого сигнала запускает всех существ на настиле по
  фиксированной дуге вправо, независимо от направления их взгляда;
- импульс подобран так, чтобы противник из любой точки патрульного маршрута
  попадал в центральную яму;
- яма Arena 04 расширена до 240 px, а крайние точки запуска проверены отдельно;
- визуальный рычаг делает короткий взмах, автоматически возвращается и только
  после проверки свободного объёма снова включает коллизию;
- Arena 04 пройдена игровым ударом по шарниру, без прямого вызова механизма.

Следующий шаг — добавить стрелка с читаемой линией атаки в Arena 05.

## 27 июля 2026 — поворачивающийся стрелок

- добавлен неподвижный стрелок, который разворачивается к игроку и ведёт оружием
  его текущую позицию;
- во время подготовки красная линия следует за целью, а перед выстрелом
  становится жёлтой и фиксирует направление, оставляя окно для уклонения;
- быстрый снаряд проверяет весь пройденный за кадр отрезок, поэтому не пролетает
  сквозь тонкие препятствия;
- снаряд отбрасывает игрока и противников, активирует шарниры и останавливается
  на первом твёрдом препятствии, не нанося прямого урона;
- над стрелком видна перезарядка, а попадание игрока отменяет незавершённое
  прицеливание;
- Arena 05 ставит между игроком и стрелком одно низкое укрытие, а яму размещает
  сразу за стрелком: нужно прочитать цикл атаки, приблизиться и столкнуть врага;
- Arena 04 теперь автоматически переводит в Arena 05.

Следующий шаг — собрать Arena 06, где выстрел стрелка должен активировать
недоступный игроку шарнир.

## 27 июля 2026 — отладочный выбор арен

- добавлено глобальное debug-меню, доступное на любой арене по `F1`;
- цифры `1`–`5` сразу загружают нужную сцену, также работают стрелки и Enter;
- во время выбора дерево сцены ставится на паузу и возвращается в прежнее
  состояние после закрытия меню;
- таймеры переходов, шарниров и платформ тоже уважают паузу и не завершают
  действия под открытым меню;
- текущая арена автоматически выделяется при открытии;
- release-сборка не показывает и не обрабатывает отладочный селектор.

## 28 июля 2026 — чужая рука

- добавлена Arena 06, в которой шарнир нельзя безопасно достать ударом игрока;
- стрелок стоит на узкой отключаемой опоре над левой ямой, а точка-приманка
  расположена у правой;
- выстрел, зафиксированный через шарнир, отключает опору и продолжает движение
  к игроку;
- после жёлтой фиксации нужно сойти с возвышения влево: оставшийся на линии
  игрок получает импульс вправо и падает;
- начальная позиция не пересекает шарнир, а собственная узкая опора не закрывает
  дуло стрелка;
- Arena 05 теперь автоматически переводит в Arena 06, а debug-меню открывает её
  клавишей `6`;
- headless-плейтест проверяет доступность приманки, отсутствие автопрохождения,
  победу с уклонением и поражение без него.

Следующий шаг — собрать Arena 07 с движущейся по вертикали платформой и
проверить, как высота меняет маршрут и линию огня.

## 28 июля 2026 — высота

- добавлен вертикальный лифт на `AnimatableBody2D` с двумя конечными положениями,
  предупреждением и видимыми направляющими;
- лифт перевозит игрока и противников средствами физики Godot, без parenting и
  ручного перемещения пассажиров;
- Arena 07 начинает со стрелком в недоступном верхнем положении: его линия огня
  проходит поверх укрытия;
- удар по шарниру опускает стрелка, после чего укрытие блокирует выстрел, а
  платформа становится достижимым островком над ямой;
- нижняя остановка доступна обычным прыжком, верхняя недостижима напрямую;
- одного удара на лифте достаточно, чтобы отправить стрелка в шахту, но его
  ответный выстрел сбрасывает туда же игрока;
- Arena 06 теперь автоматически переводит в Arena 07, а debug-меню открывает её
  клавишей `7`;
- headless-плейтест проверяет перевозку обоих персонажей, обе линии огня,
  управление шарниром, невозможность раннего обхода и полный маршрут победы.

Следующий шаг — Arena 08 «Экзамен»: короткая цепочка из уже готовых врагов и
механизмов без добавления новых систем.

## 28 июля 2026 — экзамен

- добавлена Arena 08, объединяющая патрульного, толкача и стрелка со всеми
  тремя типами платформ;
- на первом шаге игрок пересекает мост во время подготовки рывка: толкач бьёт
  собственный шарнир, выключает опору и падает в центральную яму;
- на втором шаге доступный только в прыжке шарнир запускает патрульного с
  воздушной катапульты в ту же яму;
- на третьем шаге лифт опускает стрелка за укрытие и становится доступной
  площадкой, с которой врага можно вытолкнуть в правую шахту;
- три механизма имеют отдельные шарниры и видимые кабели, а решение не требует
  новых игровых систем или исключений из общих правил;
- Arena 07 теперь автоматически переводит в Arena 08, debug-меню открывает
  экзамен клавишей `8`, а после победы финальная арена загружается заново;
- headless-плейтест проверяет десять секунд покоя, отсутствие случайных
  активаций, обычный переход по мосту, прыжковый удар, изменение линии огня,
  доступность нижнего лифта и победу над всеми тремя врагами.

Следующий шаг — провести ручные плейтесты темпа экзамена, затем настроить
визуальные и звуковые сигналы, hit-stop и скорость перезапуска.

## 29 июля 2026 — data-driven фундамент редактора

- зафиксирована архитектура будущего внутриигрового редактора уровней;
- добавлен безопасный JSON-формат `schema_version: 1` с целочисленными
  координатами, стабильными ID и закрытым списком типов и свойств;
- `LevelDataCodec` ограничивает размер файла, разбирает JSON и выдаёт
  детерминированное представление для сохранения;
- `LevelDataValidator` отвергает неизвестные поля, неверные типы, повторяющиеся
  ID, некорректный canvas, выход за границы и пересечение стартового коллайдера
  с полом, стенами или потолком;
- `LevelBuilder` создаёт прямоугольную геометрию, игрока и патрульного только из
  проверенных данных;
- добавлена отдельная `LevelRuntimeArena` с неизменяемыми стенами, потолком,
  зоной падения и игровым UI;
- Arena 01 воспроизведена в `levels/arena_01.json`, не заменяя исходную сцену и
  не вмешиваясь в существующую кампанию;
- каждая Arena теперь считает только собственных противников, что позволяет в
  будущем держать preview и playtest в одном дереве;
- smoke-тест проверяет round-trip JSON, невалидные данные, точную геометрию,
  пять секунд покоя, реальный удар, победу, падение и ручной restart.

Следующий шаг — минимальный холст редактора для пола, точки старта и
патрульного с размещением, выделением и тестовым запуском.

## 29 июля 2026 — минимальный редактор уровней

- добавлена отдельная внутриигровая сцена редактора с полным обзором арены
  960×540, сеткой 20 px и graybox-предпросмотром;
- палитра создаёт прямоугольный пол, единственную точку игрока и любое число
  патрульных, не инстанцируя игровую физику в preview;
- выбор и drag дают одно изменение истории на отпускание мыши; также работают
  сдвиг стрелками, удаление, дублирование и inspector числовых свойств;
- `LevelDraft` хранит до 100 снимков Undo/Redo и отличает чистое состояние от
  несохранённого;
- ошибки validator блокируют Save/Test, а отсутствие близкой опоры и нулевая
  скорость патруля показываются как неблокирующие предупреждения;
- `LevelStorage` принимает только безопасный `level_id`, сохраняет в
  `user://levels/` через проверенный временный файл и восстанавливаемый backup,
  поднимает новый temp после прерванной замены, сохраняет конфликтующий
  непонятный файл в quarantine, сообщает его путь в UI и затем повторно сверяет
  канонический JSON;
- New, Load и выход требуют подтверждения перед отбрасыванием изменённого
  черновика, а Save отдельно подтверждает замену другого пользовательского
  уровня;
- `F5`/Test запускает встроенный runtime из неизменяемой JSON-строки, `R`,
  падение и победа пересоздают тот же снимок, а `Esc` возвращает прежний
  черновик, выделение и Undo/Redo;
- глобальный debug-селектор подавляется на всё время редактора и playtest;
  из обычной игры редактор открывается через `F1`, затем `E`, а `← Game`
  возвращает в исходную арену;
- smoke-тест проходит create/move, Undo/Redo, invalid-state blocking,
  save/load/overwrite, restart/fall/return и отсутствие временных файлов.

Следующий шаг — добавить в формат и редактор связку `hinge → toggle_platform`,
чтобы собирать варианты Arena 02.

## 29 июля 2026 — связанные механизмы в редакторе

- schema v1 расширена типами `toggle_platform` и `hinge` без путей к сценам,
  скриптам или `NodePath`;
- платформа хранит проверяемый `rect` и стартовое состояние, шарнир — позицию и
  безопасный `target_id`;
- validator вторым проходом проверяет forward-ссылки, отсутствующие и
  несовместимые цели, полный коллайдер шарнира и строгий boolean; активные
  платформы участвуют в проверках стартовых коллизий и опоры;
- builder сначала создаёт весь набор узлов, затем атомарно разрешает связи и
  только после этого добавляет их в runtime; ручные сцены со старым
  `target_path` продолжают работать;
- runtime-платформа получает размер из JSON, а линия от шарнира до цели
  создаётся автоматически;
- в палитре появились инструменты `4 ПЛАТФОРМА` и `5 ШАРНИР`, а inspector
  позволяет выбрать стартовое состояние и переназначить target;
- постановка шарнира включает отдельный режим выбора цели на поле; `Esc`
  отменяет только связывание, не удаляя объект;
- холст показывает обычные, выбранные и сломанные связи, обновляя их по ID при
  перемещении и Undo/Redo;
- удаление платформы сохраняет dangling-ссылку как понятную блокирующую ошибку,
  а Undo восстанавливает цель;
- smoke-тесты проверяют editor-flow, deterministic round-trip,
  forward-reference, невалидные цели, динамический builder и реальный удар,
  после которого мост выключается и патрульный падает в яму.

Следующий шаг — перенести в формат толкача и стрелка, затем катапульту и
вертикальный лифт.

## 29 июля 2026 — толкач в data-driven редакторе

- schema v1 расширена типом `shove_enemy` с компактным контрактом:
  `position` и начальный `direction`;
- скорость патруля, зона обнаружения, тайминги рывка и импульс оставлены
  внутренним preset, поэтому пользовательский JSON не управляет балансом боя;
- validator считает патрульного и толкача полноценными врагами, проверяет
  точный коллайдер 32×38 и предупреждает о старте почти у края платформы;
- builder создаёт толкача под `Actors`, а сам враг лениво находит игрока среди
  соседних акторов: поведение больше не зависит от порядка объектов в JSON и
  не захватывает игрока из другой preview-арены;
- встроенный `arena_03_data` воспроизводит центральную яму, старт игрока справа
  и толкача, который начинает патруль влево;
- палитра разделена на прокручиваемые секции; `6 ТОЛКАЧ` добавляет красный
  силуэт, а Duplicate/Delete остаются закреплёнными снизу;
- preview показывает направление патруля и у выбранного толкача фактическую
  симметричную зону обнаружения ±180×±48;
- inspector разрешает менять только X, Y и направление, не раскрывая скорость
  или параметры атаки;
- smoke-тест проверяет закрытый контракт JSON, round-trip, порядок
  `shove → player`, реальную подготовку и рывок, импульс игроку, editor-flow,
  embedded playtest и победу ответным ударом в яму.

Следующий вертикальный срез редактора — стрелок без вынесения дальности,
таймингов и силы выстрела в schema.

## 29 июля 2026 — стрелок в data-driven редакторе

- schema v1 расширена типом `shooter_enemy` с минимальным контрактом
  `id`, `type`, `position`; дальность, темп и параметры снаряда остаются
  внутренним игровым preset;
- validator считает стрелка полноценным врагом, проверяет точный коллайдер
  34×38, границы арены и общую эвристику опоры;
- builder создаёт стрелка под `Actors` независимо от порядка объектов в JSON;
  сам стрелок лениво ищет игрока только среди акторов своей арены;
- снаряд теперь добавляется внутрь владеющей им `Arena`, поэтому остановка или
  restart встроенного playtest гарантированно удаляет незавершённый выстрел;
- добавлен встроенный `arena_05_data`: игрок и стрелок разделены укрытием, а
  яма за стрелком позволяет завершить уровень обычным ударом;
- `7 СТРЕЛОК` добавляет фиолетовый силуэт; inspector разрешает менять только
  X и Y, а прокручиваемая палитра автоматически показывает выбранный hotkey;
- выбранный стрелок на холсте поворачивает дуло к игроку и показывает реальную
  линию огня длиной до 900 px с коридором радиуса снаряда; пол и активные
  платформы обрезают preview, неактивные — нет;
- smoke-тест проверяет закрытый JSON-контракт, round-trip, порядок
  `shooter → player`, слежение, настоящий снаряд и импульс, блокировку
  укрытием, победу в Arena 05-like, editor-flow и lifecycle playtest.

Следующий вертикальный срез — катапульта: формат, preview траектории и
Arena 04-like пример; после неё — вертикальный лифт.

## 29 июля 2026 — катапульта в data-driven редакторе

- schema v1 расширена типом `catapult_platform` с закрытым контрактом
  `id`, `type`, `position`; размер, направление, угол, тайминги и импульс
  запуска остаются внутренним игровым preset;
- первая версия намеренно направлена только вправо: зеркальный вариант не
  добавлен в формат, пока runtime не умеет корректно зеркалить настил, зоны
  запуска и безопасную точку пассажира;
- validator проверяет точную геометрию настила 180×20 px и зоны запуска,
  границы X 32…748 и Y 94…530, опору существ и предупреждение о
  катапульте без управляющего шарнира;
- общий второй проход связей теперь разрешает
  `hinge → toggle_platform | catapult_platform`, включая forward-ссылки;
- builder создаёт существующую игровую катапульту под `Geometry`, передаёт ей
  только позицию и связывает с шарниром до запуска арены;
- добавлен встроенный `arena_04_data`, повторяющий раскладку ручной Arena 04:
  патрульный стоит на подвешенном настиле, а удар по шарниру запускает его в
  центральную яму;
- инструмент `8 КАТАПУЛЬТА` поддерживает placement, выбор, drag, nudge,
  Duplicate, Delete и Undo/Redo, удерживая весь настил в допустимой области;
  inspector намеренно показывает только X и Y;
- выбранная катапульта показывает фактическую зону запуска, поворот настила на
  −55° и первые 0,18 секунды импульса `(700, −280)` с knockback-drag цели,
  без обещания точной точки приземления;
- активная Tween-анимация теперь корректно отменяется при restart или остановке
  embedded playtest и не оставляет ожидающую coroutine;
- отдельный smoke-тест проверяет строгий JSON, границы, геометрию preview и
  runtime, forward-link, реальный запуск, победу, возврат платформы и lifecycle
  редактора.

Следующий вертикальный срез — лифт: две конечные высоты, коридор движения,
перевозка персонажей и изменение линии огня в Arena 07-like примере.

## 29 июля 2026 — вертикальный лифт в data-driven редакторе

- schema v1 расширена типом `vertical_platform` с закрытым контрактом
  `id`, `type`, `position`; позиция задаёт центр верхней остановки;
- размер 80×20 px, ход вниз на 136 px, старт сверху, предупреждение 0,25
  секунды и движение за 1 секунду остаются внутренним игровым preset;
- допустимый диапазон центра X 72…888 и Y 82…394 удерживает обе остановки
  внутри playfield и оставляет над верхним настилом место для игрока;
- validator различает начальный настил, будущий sweep корпуса и пассажирский
  clearance: существо не может стартовать под лифтом, а геометрия и другие
  механизмы — пересекать путь пассажира;
- builder создаёт существующий `VerticalPlatform` под `Geometry`, а общий
  второй проход разрешает forward-связь `hinge → vertical_platform`;
- добавлен встроенный `arena_07_data` с исходными полом, укрытием, стрелком,
  верхней остановкой и шарниром ручной Arena 07;
- инструмент `9 ЛИФТ` поддерживает placement, drag, nudge, Duplicate, Delete
  и Undo/Redo, удерживая весь коридор в безопасном диапазоне;
- preview повторяет трек, ромбы остановок и корпус runtime, а выбранный лифт
  показывает swept corridor, нижний ghost и ожидаемое положение кандидатов в
  пассажиры после оседания;
- отдельный smoke-тест проверяет строгий JSON, clearance-ошибки, builder,
  связь, перевозку игрока и стрелка, изменение линии огня, победу,
  editor-flow и остановку embedded playtest во время движения.

Следующий этап — data-driven варианты Arena 02, Arena 06 и Arena 08, затем
единый `campaign.json` вместо переходов, зашитых в сцены.

## 29 июля 2026 — Arena 02 перенесена в JSON

- добавлен встроенный `arena_02_data` между Arena 01 и Arena 03 в каталоге
  редактора;
- JSON по геймплейной геометрии повторяет левый и правый пол, центральную яму
  160 px, игрока, патрульного со скоростью 65, активный мост 160×20 px и
  шарнир;
- миграция использует существующие `solid_rect`, `patrol_enemy`,
  `toggle_platform` и `hinge`, поэтому schema v1 и runtime-код не менялись;
- физика и тайминги совпадают с ручной сценой; прямой кабель и отсутствие
  подписи над ямой остаются только визуальными различиями общего runtime;
- data-версия после победы перезапускает текущий уровень, пока переход
  Arena 02 → Arena 03 не вынесен в будущий `campaign.json`;
- linked-mechanisms smoke фиксирует точный fixture и round-trip, собирает
  настоящую Arena 02, ударом выключает мост и дожидается падения патрульного,
  а также проверяет загрузку и lifecycle встроенного playtest;
- единый linked-mechanisms smoke фиксирует положение Arena 02 между Arena 01 и
  03, а тесты остальных built-ins ищут свои примеры по стабильному ID.

Следующая миграция — Arena 06: стрелок должен активировать недоступный шарнир
и выключить собственную опору.

## 29 июля 2026 — Arena 06 перенесена в JSON

- добавлен встроенный `arena_06_data` между Arena 05 и Arena 07 в каталоге
  редактора;
- JSON повторяет основной пол, обе ямы, игрока, стрелка, его узкую активную
  опору 72×20 px и связь с недоступным шарниром;
- `solid_rect` получил необязательный флаг `one_way`: приманка 100×20 px
  пропускает игрока снизу и удерживает сверху, как в ручной Arena 06;
- положение приманки выстраивает зафиксированный прицел точно через шарнир:
  снаряд выключает опору стрелка и продолжает движение к прежней позиции
  игрока;
- общий runtime сохраняет исходные preset и тайминги стрелка, снаряда,
  шарнира и включаемой платформы;
- декоративная линия решения, отдельные подписи и переход Arena 06 → Arena 07
  остаются за пределами JSON арены.

Следующая миграция — Arena 08 с тремя противниками и всеми тремя механизмами;
после неё порядок и переходы будут вынесены в `campaign.json`.

## 29 июля 2026 — Arena 08 перенесена в JSON

- добавлен восьмой встроенный пример `arena_08_data` с точной геометрией
  экзамена: две ямы, укрытие, три врага, мост, катапульта, лифт и три связи;
- для толкача, стрелка и катапульты добавлен закрытый `behavior_preset` со
  значениями `standard` и `exam`; произвольные числовые настройки по-прежнему
  не попадают в JSON;
- `exam` сохраняет неподвижного толкача с длинной провокацией, слабый импульс
  катапульты `(180, −80)` и замедленные тайминги прицела ручной Arena 08;
- inspector переключает preset целиком, а preview меняет радиус обнаружения
  толкача и стартовую траекторию катапульты;
- физический smoke последовательно проверяет настоящий рывок по шарниру,
  падение толкача, запуск патрульного, опускание стрелка и финальный удар в
  правую шахту;
- декоративные ломаные кабели, ShieldMark и подписи шагов остаются
  presentation-отличиями общего runtime.

Все восемь арен теперь представлены в JSON. Следующий этап — единый
`campaign.json` с порядком и переходами вместо путей, зашитых в `.tscn`.

## 30 июля 2026 — data-driven кампания

- добавлен строгий корневой `campaign.json`: это единый каталог встроенных
  уровней, который задаёт порядок, стабильный ID, JSON-путь и отображаемое
  название каждой Arena 01–08;
- `CampaignStorage` перед стартом атомарно проверяет manifest и все восемь
  файлов уровней; повреждённая поздняя арена не оставляет частично играбельную
  последовательность;
- главной сценой проекта стал `CampaignRunner` с одним дочерним
  `LevelRuntimeArena`: победа в Arena 01–07 открывает следующую запись
  manifest, падение и `R` пересоздают текущую;
- `final_behavior: restart_final_level` явно фиксирует финал: после победы
  Arena 08 перезапускается и не замыкает кампанию на Arena 01;
- встроенный playtest редактора сохранил отдельный lifecycle: победа, падение и
  `R` всегда перезапускают тот же неизменяемый snapshot;
- меню `F1` строится из manifest и цифрами переключает дочерний runtime без
  возврата к ручным сценам; вход в редактор и `← Game` сохраняют текущий
  campaign level ID;
- отдельный campaign smoke фиксирует атомарный каталог Arena 01–08, переход
  Arena 01 → Arena 02, `R`, падение, прямое открытие Arena 08 и финальный
  restart внутри того же controller;
- ручные `arena_01.tscn`–`arena_08.tscn` исключены из main/debug-потока и на
  этом этапе сохранены как reference/regression-эталоны; после завершения
  миграции они удалены отдельным cleanup 31 июля.

## 30 июля 2026 — vertical slice кампании

- перед первым уровнем, обычным переходом вперёд и прямым выбором через `F1`
  добавлена короткая заставка с названием арены; `R` и падение пересоздают
  текущий runtime без её повторения;
- постоянный campaign HUD показывает положение текущей арены в
  последовательности `N / 8`;
- финальное поведение manifest изменено на `final_behavior: show_completion`:
  после победы в Arena 08 runtime удаляется, а вместо автоматического restart
  появляется отдельный экран завершения;
- на экране завершения `R` начинает новое прохождение с Arena 01, а глобальный
  debug-селектор по `F1` остаётся доступным;
- campaign smoke проверяет lifecycle заставки и HUD, обычные restart/fall,
  debug-переход, завершение и гонку `R` с clear-delay, чтобы отложенный сигнал
  уже пройденной арены не переключил новый runtime.

## 30 июля 2026 — главное меню

- новой главной сценой проекта стало меню с командами `НОВАЯ ИГРА`,
  `РЕДАКТОР УРОВНЕЙ` и `ВЫХОД`;
- первый пункт получает фокус автоматически, выбор работает через
  `↑` / `↓` и `W` / `S`, а подтверждение — через `Enter` или `Space`;
- в Web-сборке `ВЫХОД` скрыт, поскольку закрытие вкладки не является надёжной
  операцией браузерной игры;
- `НОВАЯ ИГРА` очищает запомненный debug-выбор и запускает Arena 01; позднее
  главное меню дополнено сохранением прогресса и командой `ПРОДОЛЖИТЬ`;
- `Esc` из арены или с экрана завершения возвращает в главное меню; если
  открыто меню `F1`, первое нажатие `Esc` закрывает только debug-селектор;
- редактор, открытый из главного меню, возвращается туда же, а переход через
  `F1` по-прежнему сохраняет текущую арену кампании;
- глобальный debug-селектор подавлен в главном меню и на всём протяжении
  editor/playtest lifecycle;
- отдельный main-menu smoke проверяет маршрут `меню → Новая игра → Arena 01 →
  пауза → меню → редактор → меню`, не вызывая команду `ВЫХОД`.

## 30 июля 2026 — пауза кампании

- в игровой фазе `Esc` замораживает дерево сцены и открывает отдельный overlay
  с командами `ПРОДОЛЖИТЬ`, `НАЧАТЬ АРЕНУ ЗАНОВО` и `ГЛАВНОЕ МЕНЮ`;
- повторный `Esc` продолжает игру, выбор работает через `↑` / `↓` и
  `W` / `S`, а подтверждение — через `Enter` или `Space`;
- restart из паузы пересоздаёт текущую арену без повторной стартовой заставки,
  а выход завершает текущий запуск кампании и открывает главное меню;
- глобальный debug-селектор `F1` подавлен, пока pause-overlay открыт;
- заставка новой арены и экран завершения не используют pause-overlay; на
  экране завершения `Esc` по-прежнему сразу возвращает в главное меню;
- отдельный pause-menu smoke проверяет заморозку дерева, фокус и навигацию,
  возврат в игру, restart, подавление `F1` и переход в главное меню.

## 30 июля 2026 — сохранение прогресса кампании

- добавлен `CampaignProgress` с версионированным файлом
  `user://campaign_progress.json`: версия `1` хранит текущий ID арены,
  максимально открытый ID и признак завершённой кампании;
- главное меню включает `ПРОДОЛЖИТЬ` только для корректного незавершённого
  сохранения и открывает записанную текущую арену;
- `НОВАЯ ИГРА` создаёт прогресс Arena 01, а при наличии любого save-файла —
  включая завершённый, повреждённый или несовместимый — сначала требует
  подтверждения сброса;
- естественная победа в Arena 01–07 атомарно фиксирует следующую арену как
  текущую и максимально открытую; пауза, выход в меню, `R` и restart из паузы
  сохранение не продвигают;
- победа в Arena 08 помечает прохождение завершённым и отключает
  `ПРОДОЛЖИТЬ`; подтверждённая новая игра начинает чистое прохождение;
- сохранение записывается через проверяемый временный файл и backup предыдущей
  версии, а загрузка валидирует данные и восстанавливается из корректной
  резервной копии после прерванной замены;
- первый прямой переход через `F1` отсоединяет runner от отслеживания:
  выбранная debug-арена и последующие победы в этой сессии не могут изменить
  постоянный прогресс;
- отдельного экрана выбора уровней пока нет: пользователь может начать заново
  или продолжить ровно с сохранённой арены;
- `tests/campaign_progress_smoke.gd` проверяет создание и подтверждённый сброс,
  `ПРОДОЛЖИТЬ`, checkpoint, завершение, debug-изоляцию, повреждённый JSON и
  recovery временной/резервной записи.

## 31 июля 2026 — удаление ручных арен

- удалены восемь исходных `scenes/arena_01.tscn`–`arena_08.tscn`: основной
  запуск, кампания, debug-селектор и редактор уже используют только
  `campaign.json`, JSON-уровни и общий runtime-builder;
- из linked-mechanisms smoke удалено сравнение с ручной Arena 02; те же
  размеры, позиции, параметры противника и связь шарнира уже фиксируются
  JSON-fixture, builder- и gameplay-проверками;
- универсальная runtime-сцена сохранена, а `Arena` — как базовый класс для
  `LevelRuntimeArena`;
- прежние ручные сцены остаются доступными в истории Git, без поддержки второй
  параллельной реализации уровней в рабочем дереве;
- полный набор из 12 headless smoke-тестов проходит без legacy-сцен.

## 31 июля 2026 — имя Pivotfall

- проект получил имя `Pivotfall` в настройках Godot и заголовок `PIVOTFALL` в
  главном меню;
- placeholder `GRAYBOX CAMPAIGN` заменён слоганом
  `PUSH / PIVOT / SURVIVE`;
- универсальная сцена `data_arena_01.tscn` переименована в
  `level_runtime_arena.tscn`, а все runtime-, editor- и test-ссылки переведены
  на новое нейтральное имя;
- README начинается с названия проекта и использует актуальный путь для
  standalone-запуска runtime-сцены.

## 31 июля 2026 — обратная связь удара

- успешный удар игрока создаёт короткий процедурный всплеск в направлении
  приложенного импульса; эффект не требует текстур и удаляется вместе с
  текущим runtime;
- патрульный, толкач и стрелок сразу подсвечиваются и слегка сжимаются по
  вертикали, не меняя используемый для направления знак `Visual.scale.x`;
- при попадании во врага игрок и только поражённая цель замирают на три
  physics-кадра, после чего отбрасывание продолжается с прежними параметрами;
- заморозка локальна: она не меняет `Engine.time_scale`, не останавливает
  таймеры кампании, паузу, остальные механизмы или других противников;
- удар по шарниру показывает тот же направленный всплеск, но не замораживает
  актёров;
- `tests/impact_feedback_smoke.gd` фиксирует один эффект на цель за атаку,
  направление и позицию всплеска, немедленную реакцию врага, точную длину
  заморозки, продолжение отбрасывания и восстановление визуального состояния;
- полный набор вырос до 13 headless smoke-тестов.

## 1 августа 2026 — камера и звук попадания

- общая runtime-арена получила локальный `ArenaImpactFeedback`: сигнал
  успешного удара передаёт ему точный импульс и тип цели;
- встроенная `Camera2D` меняет только экранный `offset`, поэтому физические
  позиции мира и актёров не двигаются, а интерфейс в `CanvasLayer` остаётся
  неподвижным;
- враг даёт короткий kick силой 5 px, механизм — 2.5 px; фон и нижняя полоса
  получили 8 px визуального overscan без изменения коллизий;
- звук генерируется один раз на runtime как mono 16-bit PCM, 22050 Hz и 80 мс:
  затухающая низкая волна с коротким шумовым щелчком, без внешнего `.wav`;
- враг и механизм используют разные громкость и высоту одного звука; один
  переиспользуемый `AudioStreamPlayer` не накапливает источники;
- во время паузы физика и звук останавливаются, а camera shake в режиме
  `PROCESS_MODE_ALWAYS` спокойно возвращает offset к исходному значению;
- при удалении runtime звук останавливается, stream освобождается, offset
  восстанавливается, а камера выключается до показа следующего экрана;
- игрок для подключения feedback ищется по типу среди собранных объектов, а
  не по условному ID `player_start`, поэтому пользовательские ID поддержаны;
- расширенный `impact_feedback_smoke` проверяет PCM, два preset, паузу,
  overscan, неизменность world/UI transforms и cleanup активной камеры;
- полный набор остаётся из 13 headless smoke-тестов.

## 1 августа 2026 — анимация удара игрока

- короткий удар получил процедурные фазы подготовки, контакта и возврата в
  пределах прежнего активного окна 120 мс;
- анимация меняет только локальную позу `Visual` и видимый клин
  `AttackVisual`: `CharacterBody2D`, коллайдер, зона удара, импульс,
  перезарядка и правила попадания не изменились;
- успешное попадание мгновенно доводит анимацию до контактной позы, а локальный
  hit-stop удерживает её вместе с обоими таймерами до возобновления физики;
- смещение, наклон и клин зеркалятся по направлению игрока; разворот посреди
  замаха обновляет позу в том же physics-кадре;
- завершение атаки полностью восстанавливает базовые transform и цвет, не
  сбрасывая выбранное направление;
- новый `tests/player_attack_animation_smoke.gd` фиксирует lifecycle всех
  трёх фаз, hit-stop, recovery, разворот и неизменность геометрии hitbox;
- полный набор вырос до 14 headless smoke-тестов.

## 1 августа 2026 — анимация движения игрока

- `Player` получил пять процедурных locomotion-поз: спокойное дыхание, бег с
  наклоном и шаговым ритмом, вытягивание на подъёме и падении, короткое сжатие
  при приземлении;
- landing определяется переходом состояния пола до и после `move_and_slide()`,
  а его сила — вертикальной скоростью до столкновения; floor snap и медленные
  касания не создают ложного squash;
- анимация меняет только дочерний `Visual`; transform `CharacterBody2D`, оба
  коллайдера, `AttackPivot`, параметры движения и правила столкновений остались
  прежними;
- активная атака полностью владеет визуальной позой, пока locomotion продолжает
  обновлять скрытое состояние; после recovery игрок сразу возвращается в
  актуальную позу бега или полёта без промежуточного статического кадра;
- локальный hit-stop удерживает locomotion clock, шаговый цикл, transform,
  скорость и позицию вместе с уже существующими таймерами атаки;
- новый `tests/player_locomotion_animation_smoke.gd` проверяет все пять поз,
  зеркальный наклон, attack override, hit-stop, неизменность геометрии и
  настоящее приземление на физическую платформу;
- `player_attack_animation_smoke` теперь фиксирует возврат из удара в текущую
  воздушную позу;
- полный набор вырос до 15 headless smoke-тестов.

## 1 августа 2026 — анимация толкача

- `ShoveEnemy` получил отдельные процедурные позы для патруля, подготовки,
  рывка, recovery и knockback без новых сцен, текстур или Tween;
- во время telegraph корпус сжимается и отводится от игрока, а предупреждающий
  ромб растёт; при переходе в lunge корпус резко вытягивается и наклоняется по
  направлению прежнего рывка;
- recovery начинается с короткой осевшей позы, а knockback отклоняет корпус
  против направления импульса и плавно возвращает его за прежние 280 мс;
- нижняя точка ступней остаётся на полу при squash/stretch; поза меняет только
  дочерний `Visual` и масштаб предупреждающего ромба, а `CharacterBody2D`, оба
  коллайдера и положение зоны атаки не двигаются; `AttackPivot` по-прежнему
  лишь зеркалится существующим AI в сторону рывка;
- унаследованный hit-flash по-прежнему владеет цветом и вертикальным squash,
  а state-поза последним шагом композитит свой transform с этим множителем;
  базовый `PatrolEnemy` и остальные типы противников не менялись;
- ранний return hit-stop удерживает AI-таймер, knockback, hit-flash, шаговый
  цикл, скорость, позицию и уже скомпонованную позу на прежние три кадра;
- новый `tests/shove_enemy_animation_smoke.gd` фиксирует все пять поз,
  зеркальность, state-переходы, attack lifecycle, hit feedback, hit-stop,
  неизменность AI-параметров и физической геометрии;
- существующий `shove_enemy_smoke` по-прежнему проходит реальный маршрут
  `telegraph → lunge → impulse`, Arena 03 и editor-flow;
- полный набор вырос до 16 headless smoke-тестов.

## 1 августа 2026 — анимация стрелка

- `ShooterEnemy` получил процедурные позы спокойной стойки, наведения,
  фиксации цели, отдачи, перезарядки и knockback без новых сцен, текстур,
  Tween или изменений снаряда;
- во время наведения корпус постепенно подаётся к цели и принимает устойчивую
  сжатую стойку; после фиксации поза усиливается, а оба маркера прицела
  увеличиваются, сохраняя прежние цвета и геометрию линии;
- выстрел отбрасывает только видимый корпус назад на время существующей
  вспышки дула, после чего остаётся более медленная осевшая поза, плавно
  исчезающая за прежнее время cooldown; направление отдачи фиксируется в
  момент выстрела и не перескакивает, если игрок сразу пересёк стрелка;
- knockback отклоняет корпус против направления полученного импульса, даже
  если стрелок одновременно продолжает смотреть на игрока; направление
  прицела и направление визуальной инерции хранятся раздельно, а последняя
  поза knockback точно совпадает с первой позой cooldown без нейтрального
  промежуточного кадра;
- нижняя точка ступней остаётся на месте во всех сжатых позах и при попадании;
  унаследованный hit-flash по-прежнему управляет цветом и своим squash, а
  shooter-поза композитит его множитель последним шагом;
- анимация меняет только дочерний `Visual` и масштаб двух маркеров:
  `CharacterBody2D`, коллайдер, `GunPivot`, `Muzzle`, `AimRay`, параметры AI,
  тайминги, импульс и логика снаряда не изменились;
- ранний return hit-stop удерживает AI-, knockback-, hit-flash- и
  muzzle-flash-таймеры, цикл дыхания, скорость, позицию и готовую позу на
  прежние три physics-кадра;
- новый `tests/shooter_enemy_animation_smoke.gd` фиксирует полный visual
  lifecycle, зеркальность, отдачу и cooldown, раздельные aim/impulse
  направления, композицию попадания и неизменность боевой геометрии;
- существующие `shooter_enemy_smoke` и `impact_feedback_smoke` по-прежнему
  проходят реальный выстрел, укрытия, Arena 05–06 и базовый feedback;
- полный набор вырос до 17 headless smoke-тестов.

## 1 августа 2026 — анимация базового патрульного

- `PatrolEnemy` получил простую механическую походку: квадратный корпус
  попеременно приподнимается, слегка вытягивается и качается вокруг прежней
  траектории движения;
- разворот у края или стены запускает короткую 140-мс позу торможения после
  прежнего мгновенного `_turn_around()`: корпус оседает и отстаёт от нового
  направления, но скорость и AI не получают дополнительной паузы;
- knockback немедленно сбрасывает старую фазу шага, отводит корпус против
  направления импульса и после окончания естественно возвращается через
  нейтральную позу; столкновение со стеной во время knockback не оставляет
  отложенную turn-анимацию;
- walk-bob намеренно поднимает ступни вместе с корпусом, а в turn-, knockback-
  и hit-squash-позах их нижняя точка остаётся закреплена; цвет по-прежнему
  полностью принадлежит общему hit-flash;
- процедурная поза меняет только дочерний `Visual`: `CharacterBody2D`,
  коллайдер, `EdgeProbe`, скорость патруля, гравитация, drag и длительность
  knockback не изменились;
- базовый compositor защищён виртуальным opt-out: `ShoveEnemy` и
  `ShooterEnemy` сохраняют собственные позы даже внутри вызовов `super()`, а
  общий hit-flash остаётся единственным совместным визуальным слоем;
- ранний return hit-stop удерживает knockback, hit-flash, turn-таймер, фазу
  шага, скорость, позицию и готовую позу на прежние три physics-кадра;
- новый `tests/patrol_enemy_animation_smoke.gd` фиксирует походку,
  зеркальность, turn recovery, естественный выход из knockback, композицию
  попадания, геометрию и opt-out обоих наследников;
- существующие `impact_feedback_smoke`, `shove_enemy_animation_smoke` и
  `shooter_enemy_animation_smoke` подтверждают совместимость общего feedback
  и child-compositor;
- полный набор вырос до 18 headless smoke-тестов.

## 1 августа 2026 — анимация связанных механизмов

- принятый удар по `Hinge` теперь сразу сжимает и встречным вращением
  разводит два декоративных ромба; направление берётся из импульса, а для
  нулевого импульса используется стабильное направление вправо;
- после короткого нажатия внутренний ромб спокойно пульсирует, пока шарнир
  ждёт одновременно завершения cooldown и связанного механизма; unlock точно
  восстанавливает исходные position, rotation и scale;
- обычная, поворотная и вертикальная платформы используют один
  `MechanismWarningPulse`: на принятом запросе оранжевый контур расширяется и
  затухает в пределах уже существующего warning-окна;
- helper меняет только `Line2D.width`; цвета остаются у конкретной платформы,
  а root, `VisualPivot`, движущийся `Platform`, preview, коллизии, импульсы,
  тайминги и порядок сигналов не изменились;
- повторный запрос во время перехода не перезапускает визуальную фазу;
  `TogglePlatform` сбрасывает её при переключении, `RotatingPlatform` — перед
  запуском, `VerticalPlatform` — перед началом движения;
- новый `tests/mechanism_animation_smoke.gd` фиксирует immediate feedback,
  восстановление позы и контура, rejected/busy запросы, обе стороны unlock,
  неизменность геометрии и границы warning-фазы всех трёх платформ;
- существующие `linked_mechanisms_smoke`, `catapult_platform_smoke`,
  `vertical_platform_smoke` и `impact_feedback_smoke` по-прежнему проверяют
  полные игровые lifecycle и взаимодействие с игроком;
- полный набор вырос до 19 headless smoke-тестов.

## 1 августа 2026 — feedback ямы и падения

- над `PitBand` появилась тонкая янтарная `PitEdge`: она медленно дышит от
  2 до 2,35 px и благодаря слою под геометрией видна только в реальных
  разрывах между платформами;
- новый `ArenaHazardFeedback` при падении на 180 мс усиливает кромку примерно
  до 5 px и кратко подкрашивает глубину ямы; компонент не создаёт таймеров,
  не меняет outcome и владеет только цветом `PitBand`, цветом и шириной линии;
- `Player` получил отдельный `FallVisualPivot`: при первом входе в
  `DeathZone` он сохраняет global transform и становится top-level, поэтому
  физическое тело продолжает падать, а силуэт остаётся читаемым в проёме;
- за прежние 450 мс силуэт получает короткий entry-squash, опускается на
  40 px, поворачивается примерно на 18 градусов, уменьшается до 8% и исчезает;
  root, velocity, движение, `AttackPivot`, `AttackArea` и коллайдер не
  участвуют в visual compositor;
- `Arena` сначала фиксирует прежний `FALL` outcome и актуальную restart
  generation, затем запускает необязательные визуальные эффекты; повторный
  вход не сбрасывает прогресс и не создаёт новый timer;
- отдельный `tests/hazard_feedback_smoke.gd` фиксирует idle/alert-фазы ямы,
  fall-out curve, idempotency, top-level anchor, геометрию и cleanup;
- существующие foundation, campaign, editor, player animation и linked
  mechanisms smoke-тесты сохраняют прежние физические падения и restart-flow;
- полный набор вырос до 20 headless smoke-тестов.

## 1 августа 2026 — смертельные враги и атаки

- `Player` получил единый идемпотентный контракт поражения с причиной
  `COMBAT` или `FALL`; повторные попадания не создают дополнительные таймеры;
- корпуса патрульного, толкача и стрелка получили отдельную non-physical
  контактную зону: она видит только слой игрока и не меняет прежнее свободное
  прохождение тел друг сквозь друга;
- касание любого врага, рывок толкача и снаряд стрелка теперь сразу поражают
  игрока вместо отбрасывания; импульс по другим врагам и шарнирам сохранён;
- боевое поражение использует прежний `FALL` lifecycle и задержку restart, но
  показывает статус `ПОРАЖЕНИЕ` и не запускает pit-only flash кромки ямы;
- старые вертикальные smoke-срезы толкача и стрелка теперь проверяют смертельное
  попадание, а foundation idle-сценарий изолирован от намеренного контакта;
- новый `tests/lethal_enemy_smoke.gd` фиксирует контакт всех трёх врагов,
  отдельную атаку толкача, настоящий ray projectile и единичный outcome арены;
- полный набор вырос до 21 headless smoke-теста.

## 1 августа 2026 — feedback боевого поражения

- lethal-контракт теперь передаёт нормализованное направление первого
  источника: от корпуса врага к игроку, вдоль рывка толкача или полёта снаряда;
- в том же вызове `Player` сбрасывает запросы прыжка и атаки, закрывает активный
  hitbox и перестаёт принимать ввод, импульсы и отложенные попадания;
- combat-поражение удерживает силуэт три локальных physics-кадра, затем уводит
  только `FallVisualPivot` по направленной короткой дуге с яркой коралловой
  вспышкой, поворотом, уменьшением и fade; физический root и velocity не
  меняются;
- падение в яму сохранило прежний физический и визуальный путь, включая
  `ArenaHazardFeedback`; combat и pit больше не используют одну позу;
- `ArenaImpactFeedback.play_defeat()` добавляет более сильный camera kick и
  более низкий процедурный звук, не меняя `Engine.time_scale`, world/UI
  transforms или существующие пресеты удара игрока;
- общий HUD теперь заранее сообщает, что враги и их атаки смертельны;
- lethal, impact и hazard smoke-тесты фиксируют control lock, три направления,
  отдельные позы, idempotency, audio/camera cleanup и отсутствие перекрёстных
  pit/combat-эффектов; полный набор остаётся из 21 headless smoke-теста.

## 1 августа 2026 — feedback устранения врага и победы

- враг по-прежнему засчитывается и удаляется сразу при входе в `DeathZone`,
  поэтому физика, JSON-формат, builder, campaign transition и сохранение не
  получили нового промежуточного состояния;
- группа `enemies` снимается синхронно до deferred `queue_free()`: повторный
  callback того же тела в одном physics-кадре больше не может дважды уменьшить
  счётчик или преждевременно завершить арену;
- на месте устранения остаётся короткий процедурный ромб-силуэт с кольцом и
  лучами: он наследует основной цвет врага, движется в яму по направлению его
  скорости, вращается, уменьшается и исчезает как pausable world-эффект;
- обычное устранение даёт бирюзовый pulse кромки, высокий звук и слабый camera
  kick; последний враг получает увеличенный зелёный burst, отдельный clear
  pitch, более сильный kick и единственный `CLEAR` outcome;
- camera/audio preset объединён в один вызов на врага, поэтому финальный звук
  не перебивает второй дублирующий elimination-сигнал; player-hit и defeat
  счётчики остаются независимыми;
- новый `tests/enemy_elimination_feedback_smoke.gd` проверяет все три типа,
  duplicate suppression, нулевую скорость, обычный и clear preset, паузу,
  неизменность world/UI/DeathZone/Pit геометрии и cleanup активного runtime;
  полный набор вырос до 22 headless smoke-тестов.

## 1 августа 2026 — победная поза игрока

- последний враг теперь переводит `Player` в отдельное terminal-состояние
  празднования: активная атака закрывается, запросы управления, velocity,
  knockback и локальный hit-stop очищаются синхронно, а поза бега, прыжка или
  приземления сначала нормализуется в устойчивый idle;
- только `FallVisualPivot` делает короткий squash с опорой на нижнюю грань,
  один hop и мягкий лаймовый hold; физический root, коллайдер и геометрия
  арены остаются неподвижны, глобальный `Engine.time_scale` не используется;
- clear-состояние не принимает новый ввод, импульсы, атаки и lethal hits, а
  его финальная поза удерживается до смены runtime и корректно замирает вместе
  с деревом при паузе;
- outcome-арбитраж закрепляет правило «первый terminal outcome выигрывает»:
  после `CLEAR` поздняя яма, снаряд или defensive defeat callback не могут
  заменить победу поражением либо создать новый таймер;
- расширенный `enemy_elimination_feedback_smoke` проверяет immediate lock,
  все поздние источники поражения, duplicate begin, фазы hop/hold, паузу,
  неизменность gameplay-геометрии и cleanup; полный набор остаётся из 22
  headless smoke-тестов.

## 1 августа 2026 — обмен пользовательскими уровнями

- в панели уровней появились `IMPORT` и `EXPORT`: текущий валидный draft можно
  скачать как канонический `<level_id>.json`, а полученный файл — добавить в
  пользовательскую библиотеку и сразу открыть;
- отдельный `LevelFileTransfer` использует `JavaScriptBridge.download_buffer`
  и browser file picker в Web, а на desktop — системные файловые диалоги;
- импорт ограничивает файл 256 KiB, не доверяет внешнему имени и пропускает
  данные через существующие codec, validator и атомарный `LevelStorage`;
- несохранённый draft и конфликт пользовательского ID объединяются в одно
  явное подтверждение; отмена сохраняет оба исходных состояния;
- новый `tests/level_import_export_smoke.gd` фиксирует канонический export,
  повреждённый и слишком большой import, dirty/conflict cancel и атомарную
  замену; полный набор вырос до 24 headless smoke-тестов.
